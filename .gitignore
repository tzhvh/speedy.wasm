# Documentation files (to avoid SBOM confusion)
docs/*.md

# Build directories
dist/
build/

# Native build artifacts
*.o
*.so
speedy_wave

# WebAssembly files (but allow in public/ for GitHub Pages)
*.wasm
!public/*.wasm

# Dependency build artifacts
deps/*/build/
deps/*/*.o
deps/*/*.so
